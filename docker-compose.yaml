version: '3.8'
services:
  app:
    build: .
    container_name: flask_tratamiento_datos
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: python run_app.py
