version: '3.8'
services:
  app:
    build: .
    container_name: flask_tratamiento_datos
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    command: python app.py
